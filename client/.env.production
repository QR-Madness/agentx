# Production web/PWA build (Cloudflare Pages at agx.thejpnet.net).
# Public URL — NOT a secret. Read by lib/connectionString.ts so share links
# (and desktop-issued #connect= links, where window.location.origin is the
# unshareable tauri://localhost) resolve to the hosted app.
VITE_PUBLIC_APP_URL=https://agx.thejpnet.net
