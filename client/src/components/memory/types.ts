// Memory Workbench areas (top tab bar). `overview` is the landing tab;
// `explore` is the graph canvas (formerly `graph`); `procedures` surfaces
// distilled procedural memory.
export type MemoryArea =
  | 'overview'
  | 'entities'
  | 'facts'
  | 'strategies'
  | 'procedures'
  | 'explore'
  | 'user-history'
  | 'jobs';
