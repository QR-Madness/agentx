{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for AgentX main window. Only core Tauri functionality and URL opening are enabled. No filesystem, shell, or other privileged access is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
