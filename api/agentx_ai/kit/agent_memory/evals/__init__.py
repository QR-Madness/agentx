"""Evaluation corpora + scoring for the memory eval harnesses.

``recall_corpus`` — golden-set dataclasses + the builtin corpus for ``eval_recall``.
``recall_scoring`` — pure ranking-metric functions (no Django/Neo4j imports).
"""
