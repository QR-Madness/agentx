"""Graph maintenance utilities (entity merge, repair helpers).

Shared by management commands (``dedupe_entities``) and — later — the
consolidation-time adjudicator / memory janitor (Memory-Roadmap §1.2, §2.10).
"""
